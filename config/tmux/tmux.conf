# Base settings
set -g mouse on
# set -g default-terminal "tmux-256color"
# set -g default-terminal "screen-256color"
set -g default-terminal "tmux-256color"
# set-window-option -g utf8 on
set -g base-index 1
set -g renumber-windows on
set -g allow-rename off
set -g status-position top
set -g pane-base-index 1
set -ga terminal-overrides ",*256col*:Tc"
set -s extended-keys on
set-window-option -g mode-keys vi

# Responsiveness — kill the 500ms Esc delay (critical for zsh-vi-mode).
set -sg escape-time 10
set -g focus-events on
set -g history-limit 50000

# Prefix key
unbind-key C-b
set -g prefix C-a
bind-key C-a send-prefix

# Plugins
set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'omerxx/tmux-floax'
set -g @plugin 'laktak/extrakto'

bind C-r source-file ~/.config/tmux/tmux.conf \; display "Tmux config reloaded!"

source-file ~/.config/tmux/binds.conf
source-file ~/.config/tmux/statusbar.conf

# bootstrap tpm (paths must match the install location below)
if-shell "test ! -d ~/.config/tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"

# TPM Plugin Manager
run '~/.config/tmux/plugins/tpm/tpm'
